/**
 * Application version. Part of the PluralKit User-Agent, so bumping it is
 * visible to PluralKit's maintainers — bump on meaningful behaviour changes to
 * our API usage, not on every deploy.
 */
export const PKVIEWER_VERSION = "0.1.0";
